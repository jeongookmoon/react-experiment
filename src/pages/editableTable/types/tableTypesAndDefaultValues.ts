export type rowProps = {
  id: number;
  first_name: string;
  last_name: string;
  email: string;
  gender: string;
  ip_address: string;
};

export const defaultJobsStatus = {
  add: false,
  edit: false,
  delete: false,
};
